<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.ex03.mapper.NoticeMapper">
	
	<!-- 글 작성 -->
	<insert id ="insertNotice" parameterType="NoticeBoardVO">
		INSERT INTO 
			NOTICEBOARD (id, category, title, ) 
		VALUES(#{id}, #{password}, #{name}, #{email,jdbcType=VARCHAR}, #{enter, jdbcType=INTEGER})
	</insert>
	
</mapper>